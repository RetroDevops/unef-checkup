# Build performance
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.caching=true
org.gradle.parallel=true

# This app uses only the Android framework SDK (no AndroidX / support libraries),
# which keeps the APK tiny and dependency-free.
android.useAndroidX=false
